# DO NOT CHANGE THIS FILE!
# This config is generated by SALTSTACK
# and all change will be overrided on next salt call

{% from "mysql/default.yml" import rawmap with context -%}
{%- set mysql = salt['grains.filter_by'](rawmap, grain='os', merge=salt['pillar.get']('mysql')).config -%}

{%- for s_name, s_items in mysql.sections.items() -%}
[{{s_name}}]
    {%- for i_name, i_value in s_items.items() %}
    {%- if i_value == "noarg_present" %}
    {{i_name}}
    {%- else %}
    {{i_name}} = {{i_value}}
    {%- endif -%}
    {% endfor %}
{%- endfor -%}
